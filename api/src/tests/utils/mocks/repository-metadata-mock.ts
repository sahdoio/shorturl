import { RepositoryMetadata } from '../../../app/data/protocols/repositories/repository'

export const repositoryMetadataMock: RepositoryMetadata = {
  currentPage: 1,
  itemsPerPage: 1,
  totalOfPages: 1,
  totalOfRegisters: 1
}
